import{_ as a,o as s,c as n,ae as t}from"./chunks/framework.CFUUUNbY.js";const u=JSON.parse('{"title":"Namespace Resolution Protocol v0.1 ​","description":"","frontmatter":{},"headers":[],"relativePath":"Namespace-Resolution-Protocol.md","filePath":"Namespace-Resolution-Protocol.md"}'),o={name:"Namespace-Resolution-Protocol.md"};function i(r,e,c,p,l,d){return s(),n("div",null,[...e[0]||(e[0]=[t(`<h1 id="namespace-resolution-protocol-v0-1-​" tabindex="-1">Namespace Resolution Protocol v0.1 ​ <a class="header-anchor" href="#namespace-resolution-protocol-v0-1-​" aria-label="Permalink to &quot;Namespace Resolution Protocol v0.1 ​&quot;">​</a></h1><p><strong>neurons.me / suiGn ** ** Status:</strong> Draft — Working Document <strong>License:</strong> CC0 1.0 Universal — Public Domain</p><hr><h2 id="preamble-​" tabindex="-1">Preamble ​ <a class="header-anchor" href="#preamble-​" aria-label="Permalink to &quot;Preamble ​&quot;">​</a></h2><p>This document specifies the <strong>Namespace Resolution Protocol (NRP)</strong> for the <code>me://</code> URI scheme.</p><p>The NRP defines how a <code>me://</code> URI is resolved from a symbolic address into a concrete semantic value, across a distributed mesh of surfaces — without a central registry, without a central server, and without requiring persistent connectivity.</p><p>It is the protocol that closes the gap between:</p><ul><li><strong>Semantic resolution</strong> — already implemented in <code>this.me</code>: local, mathematical, derivation-based. The kernel resolves <code>me.get(&quot;wallet.balance&quot;)</code> entirely offline.</li><li><strong>Topological resolution</strong> — the new work: how a requesting surface finds and reaches the surface that holds the target namespace, path, and key material. These two layers are separate concerns, implemented separately, but always composed in that order:</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[surface:iphone]/wallet.balance</span></span>
<span class="line"><span>         │                    │                 │</span></span>
<span class="line"><span>    topological           topological        semantic</span></span>
<span class="line"><span>    (find the             (find which        (resolve the</span></span>
<span class="line"><span>     namespace)            surface)           path locally)</span></span></code></pre></div><p>The NRP specifies the topological layer. The semantic layer is already specified by <code>this.me</code>.</p><hr><h2 id="_1-definitions-​" tabindex="-1">1. Definitions ​ <a class="header-anchor" href="#_1-definitions-​" aria-label="Permalink to &quot;1. Definitions ​&quot;">​</a></h2><p><strong>Namespace</strong> — A named semantic domain. Represented as a human-readable label (e.g., <code>jabellae.cleaker.me</code>). A namespace is owned by whoever holds its root key material. There is no central authority that grants or revokes a namespace.</p><p><strong>Surface</strong> — A physical or logical runtime context that can hold a <code>.me</code> kernel instance and participate in the mesh. Examples: an iPhone app, a MacBook daemon, a server process, a browser tab. A surface has a name within a namespace.</p><p><strong>Surface identity</strong> — The cryptographic identifier of a surface within a namespace:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>surface_id = hash(namespace_key, surface_name)</span></span></code></pre></div><p>This is never the cleartext <code>surface_name</code>. Only holders of <code>namespace_key</code> can derive or verify a <code>surface_id</code>.</p><p><strong>Namespace key</strong> — The root secret from which all key material in a namespace is derived. Equivalent to the seed in the derivation-based identity model. Never transmitted; always local.</p><p><strong>Endpoint descriptor</strong> — A transport-layer locator for a surface (IP:port, relay address, onion address, etc.). Stored encrypted inside the surface index. Not the surface&#39;s identity — just how to reach it right now.</p><p><strong>Surface index</strong> — A <code>.me</code> secret space, scoped under the namespace key, that maps surface identities to their current endpoint descriptors. It is the mesh&#39;s contact book. Every surface that holds the namespace key can read and write it.</p><p><strong>Claim token</strong> — A one-time, time-limited token that authorizes a new surface to join a namespace. Generated by an existing surface. Expires. Consumed on first use.</p><p><strong>Stealth root</strong> — A secret scope root that returns <code>undefined</code> on resolution. This is an honest absence, not an error. The NRP must preserve this distinction at the network level.</p><p><strong>Semantic island</strong> — A self-contained <code>.me</code> kernel instance holding a fragment of a namespace. Islands do not need to be constantly connected. They interact by exchanging mathematical hashes, not cleartext data.</p><hr><h2 id="_2-the-two-layer-model-​" tabindex="-1">2. The Two-Layer Model ​ <a class="header-anchor" href="#_2-the-two-layer-model-​" aria-label="Permalink to &quot;2. The Two-Layer Model ​&quot;">​</a></h2><p>Resolution of a <code>me://</code> URI proceeds in two phases, always in this order.</p><h3 id="phase-1-—-topological-resolution-​" tabindex="-1">Phase 1 — Topological resolution ​ <a class="header-anchor" href="#phase-1-—-topological-resolution-​" aria-label="Permalink to &quot;Phase 1 — Topological resolution ​&quot;">​</a></h3><p><strong>Input:</strong> <code>namespace</code> + <code>selector</code><strong>Output:</strong> a reachable surface endpoint</p><p>The requesting surface must find the surface that can serve the requested path. This involves:</p><ul><li>Deriving the target <code>surface_id</code> from the local namespace key and the selector&#39;s surface name.</li><li>Looking up the endpoint descriptor for that <code>surface_id</code> in the local surface index.</li><li>Establishing a connection to that endpoint. If the surface index does not contain the target <code>surface_id</code>, or if the target surface is unreachable, topological resolution fails. The request does not proceed to Phase 2.</li></ul><h3 id="phase-2-—-semantic-resolution-​" tabindex="-1">Phase 2 — Semantic resolution ​ <a class="header-anchor" href="#phase-2-—-semantic-resolution-​" aria-label="Permalink to &quot;Phase 2 — Semantic resolution ​&quot;">​</a></h3><p><strong>Input:</strong> <code>path</code> + key material (if the path is within a secret scope) <strong>Output:</strong> the resolved value, or <code>undefined</code>, or a closed failure</p><p>Once the requesting surface has a connection to the target surface, it sends a read request for the path. The target surface resolves the path locally using its <code>.me</code> kernel, and returns a <strong>disclosure envelope</strong> (see Section 6).</p><p>Semantic resolution is entirely local to the target surface. The requesting surface never sees the target&#39;s kernel state directly — only the disclosure envelope for the specific path requested.</p><hr><h2 id="_3-surface-identity-and-the-surface-index-​" tabindex="-1">3. Surface Identity and the Surface Index ​ <a class="header-anchor" href="#_3-surface-identity-and-the-surface-index-​" aria-label="Permalink to &quot;3. Surface Identity and the Surface Index ​&quot;">​</a></h2><h3 id="_3-1-surface-identity-derivation-​" tabindex="-1">3.1 Surface identity derivation ​ <a class="header-anchor" href="#_3-1-surface-identity-derivation-​" aria-label="Permalink to &quot;3.1 Surface identity derivation ​&quot;">​</a></h3><p>A surface&#39;s identity within a namespace is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>surface_id = HMAC-SHA256(namespace_key, &quot;surface:&quot; + surface_name)</span></span></code></pre></div><p>This produces a 32-byte opaque identifier. It is the surface&#39;s public identity on the mesh. Parties without the <code>namespace_key</code> cannot derive it, verify it, or enumerate surfaces in the namespace.</p><p>The <code>surface_name</code> is a human-readable label chosen by the surface operator (e.g., <code>&quot;iphone&quot;</code>, <code>&quot;macbook&quot;</code>, <code>&quot;daemon-home&quot;</code>). It is meaningful only to namespace holders.</p><h3 id="_3-2-surface-index-structure-​" tabindex="-1">3.2 Surface index structure ​ <a class="header-anchor" href="#_3-2-surface-index-structure-​" aria-label="Permalink to &quot;3.2 Surface index structure ​&quot;">​</a></h3><p>The surface index is a <code>.me</code> secret space:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;_&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">](</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;namespace_key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// For each known surface:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[surface_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;iphone&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[surface_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">endpoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;encrypted_endpoint_descriptor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[surface_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">last_seen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(timestamp);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[surface_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">public_key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;surface_ephemeral_pubkey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>The index is stored locally on every surface that holds the namespace key. There is no canonical remote copy. Surfaces sync the index through the mesh on reconnection (see Section 7).</p><p>The <code>endpoint_descriptor</code> field is doubly encrypted: first under the namespace key (so only namespace members can read it), and second it may use an ephemeral surface key for transport security. The endpoint is volatile and may change. The <code>surface_id</code> is stable.</p><h3 id="_3-3-reading-from-the-surface-index-​" tabindex="-1">3.3 Reading from the surface index ​ <a class="header-anchor" href="#_3-3-reading-from-the-surface-index-​" aria-label="Permalink to &quot;3.3 Reading from the surface index ​&quot;">​</a></h3><p>A surface resolves <code>me://[surface:iphone]/some.path</code> as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Compute: target_id = HMAC-SHA256(namespace_key, &quot;surface:iphone&quot;)</span></span>
<span class="line"><span>2. Read: endpoint = me(&quot;surfaces.&quot; + target_id + &quot;.endpoint&quot;)</span></span>
<span class="line"><span>3. If endpoint is undefined → surface not found → fail with NRP_ERROR_SURFACE_NOT_FOUND</span></span>
<span class="line"><span>4. Decrypt endpoint descriptor → get transport address</span></span>
<span class="line"><span>5. Proceed to Phase 2 (semantic resolution)</span></span></code></pre></div><hr><h2 id="_4-the-claim-ceremony-​" tabindex="-1">4. The Claim Ceremony ​ <a class="header-anchor" href="#_4-the-claim-ceremony-​" aria-label="Permalink to &quot;4. The Claim Ceremony ​&quot;">​</a></h2><p>The claim ceremony is how a new surface joins a namespace. It replaces the need for a central authority to &quot;add&quot; a surface.</p><p>The current implementation has a claim token as a temporary in-memory value. This section formalizes and hardens it.</p><h3 id="_4-1-token-generation-​" tabindex="-1">4.1 Token generation ​ <a class="header-anchor" href="#_4-1-token-generation-​" aria-label="Permalink to &quot;4.1 Token generation ​&quot;">​</a></h3><p>An existing surface in the namespace (the <strong>inviting surface</strong>) generates a claim token:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>nonce         = random_bytes(16)</span></span>
<span class="line"><span>expiry        = now() + TTL            // recommended TTL: 300 seconds</span></span>
<span class="line"><span>claim_token   = HMAC-SHA256(namespace_key, nonce + expiry)</span></span>
<span class="line"><span>claim_payload = base64url(nonce + expiry + claim_token)</span></span></code></pre></div><p>The claim payload is encoded into the URI:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[claim:CLAIM_PAYLOAD]/new-surface</span></span></code></pre></div><p>And optionally rendered as a QR code for physical proximity pairing (already implemented in Cleaker).</p><h3 id="_4-2-token-verification-​" tabindex="-1">4.2 Token verification ​ <a class="header-anchor" href="#_4-2-token-verification-​" aria-label="Permalink to &quot;4.2 Token verification ​&quot;">​</a></h3><p>The <strong>new surface</strong> presents the claim payload. The inviting surface (or any namespace surface that receives the pairing request) verifies:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Decode claim_payload → extract nonce, expiry, presented_token</span></span>
<span class="line"><span>2. Check: now() CodeMeaning\`NRP_ERROR_SURFACE_NOT_FOUND\`\`surface_id\` not in local index\`NRP_ERROR_SURFACE_UNREACHABLE\`surface_id found but endpoint not reachable\`NRP_ERROR_CLAIM_EXPIRED\`claim token TTL exceeded\`NRP_ERROR_CLAIM_INVALID\`HMAC verification failed\`NRP_ERROR_CLAIM_CONSUMED\`nonce already used\`NRP_ERROR_NAMESPACE_UNKNOWN\`no namespace key available for this namespace\`NRP_ERROR_PATH_NOT_FOUND\`path does not exist and is not near any secret scope\`NRP_ERROR_TRANSPORT\`network-level failure (connection refused, timeout, etc.)</span></span>
<span class="line"><span>---</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## 9. What Is Not Specified Here (v0.1 scope) ​</span></span>
<span class="line"><span></span></span>
<span class="line"><span>The following are intentionally deferred to later versions:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Transport binding** — This document specifies the protocol semantics, not the wire format. A v0.2 transport binding document will specify the exact message encoding (JSON-over-WebSocket, protobuf, etc.) and the TLS/noise handshake for the surface-to-surface connection.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Relay infrastructure** — When two surfaces cannot reach each other directly (NAT, firewall, different networks), a relay is needed. The relay protocol is not specified here. The relay must not be able to read message content — it is a blind forwarder. Relay discovery and selection are deferred.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Namespace key distribution across surfaces** — This document assumes the namespace key is already present on the requesting surface. The initial key bootstrap (how the first surface gets the namespace key from the derivation seed) is specified by the \`.me\` kernel, not the NRP. Subsequent key sharing is handled by the claim ceremony (Section 4).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Multi-namespace resolution** — A surface may hold keys for multiple namespaces. How a surface selects the correct namespace key when a URI omits the namespace qualifier is implementation-defined in v0.1.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**Revocation** — How to revoke a surface from a namespace (e.g., a lost device) without a central authority is an open problem. A candidate mechanism is a signed revocation entry in the surface index, broadcast to all surfaces. Deferred to v0.2.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>---</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## 10. Design Principles ​</span></span>
<span class="line"><span></span></span>
<span class="line"><span>These are the invariants that any implementation must preserve. They are not implementation details — they are the protocol&#39;s soul.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**1. No central registry.** The surface index is local to each namespace holder. Discovery requires holding the namespace key. There is nothing to hack that reveals a complete list of surfaces or identities.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**2. Identity is computation, not storage.** A surface&#39;s identity is derived from a key and a name. It is not assigned by an authority and not stored in a global directory.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**3. Honest absence.** A stealth root returns \`undefined\`, not an error. The protocol does not reveal the existence of secret scopes to parties without the key. Absence and secrecy are indistinguishable from the outside.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**4. Fail closed.** Wrong keys produce the same response as absent keys. The protocol leaks no information about why a resolution returned \`undefined\`.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**5. Minimum surface area.** Surfaces exchange only what is necessary. A broadcast sends a path read request — not the full kernel state, not the surface index, not any metadata beyond what the request requires.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**6. Mathematical fragmentation.** No single surface holds a complete picture of any namespace. The identity is distributed across surfaces by design. Stealing one surface does not compromise the namespace.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>**7. Offline-capable.** Local resolution (\`[current]\` or no selector) works with no network access. The protocol degrades gracefully: offline surfaces are skipped in broadcast, missing surfaces return \`NRP_ERROR_SURFACE_UNREACHABLE\`. The local kernel is never blocked by network state.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>---</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## Appendix A — Formal Grammar (ABNF, from URI scheme v1) ​</span></span>
<span class="line"><span></span></span>
<span class="line"><span>\`\`\`txt</span></span>
<span class="line"><span>me-uri       = &quot;me://&quot; [ namespace ] [ selector ] [ &quot;/&quot; path ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>namespace    = 1*( ALPHA / DIGIT / &quot;.&quot; / &quot;_&quot; / &quot;-&quot; )</span></span>
<span class="line"><span>selector     = &quot;[&quot; ( &quot;current&quot; / &quot;&quot; / &quot;surface:&quot; surface-name / &quot;claim:&quot; token ) &quot;]&quot;</span></span>
<span class="line"><span>surface-name = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span>
<span class="line"><span>token        = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span>
<span class="line"><span>path         = *( VCHAR / &quot;/&quot; )</span></span></code></pre></div><p>The <code>secret:key@namespace</code> prefix for authenticated access is parsed as:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me-uri-auth  = &quot;me://&quot; &quot;secret:&quot; secret-key &quot;@&quot; namespace [ selector ] [ &quot;/&quot; path ]</span></span>
<span class="line"><span>secret-key   = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span></code></pre></div><hr><h2 id="appendix-b-—-open-questions-for-v0-2-​" tabindex="-1">Appendix B — Open Questions for v0.2 ​ <a class="header-anchor" href="#appendix-b-—-open-questions-for-v0-2-​" aria-label="Permalink to &quot;Appendix B — Open Questions for v0.2 ​&quot;">​</a></h2><p>These are the questions this document intentionally leaves open, in priority order:</p><ul><li><strong>Relay protocol</strong> — How do two surfaces on different networks find a common relay, and how does the relay stay blind?</li><li><strong>Surface revocation</strong> — How does a namespace holder revoke a lost or compromised surface without a central authority?</li><li><strong>Namespace discovery</strong> — Can a surface discover other namespaces it does not already hold keys for? (Current answer: no. Is this the right answer forever?)</li><li><strong>Index consistency under partition</strong> — If the mesh is partitioned for a long time and surfaces update their own entries, what is the merge strategy beyond LWW?</li><li><strong>Derivation-based namespace bootstrap</strong> — Should the namespace key be derivable from a BIP-39 mnemonic or similar standard seed phrase, so it can be reconstructed without any network access?</li><li><strong>Cross-namespace pointers</strong> — The <code>[&quot;-&gt;&quot;]</code> operator in <code>.me</code> can create semantic links between namespaces. How does the NRP resolve a path that crosses namespace boundaries?</li></ul><hr><p><strong>∴ Witness our seal<strong><strong>suiGn / neurons.me</strong></strong>v0.1 — Draft</strong></p>`,70)])])}const m=a(o,[["render",i]]);export{u as __pageData,m as default};
