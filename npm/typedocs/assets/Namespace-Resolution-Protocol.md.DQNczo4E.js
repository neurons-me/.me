import{_ as a,o as s,c as t,ae as n}from"./chunks/framework.CY_fclge.js";const u=JSON.parse('{"title":"Namespace Resolution Protocol v0.1","description":"","frontmatter":{},"headers":[],"relativePath":"Namespace-Resolution-Protocol.md","filePath":"Namespace-Resolution-Protocol.md"}'),i={name:"Namespace-Resolution-Protocol.md"};function o(r,e,l,c,p,h){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="namespace-resolution-protocol-v0-1" tabindex="-1">Namespace Resolution Protocol v0.1 <a class="header-anchor" href="#namespace-resolution-protocol-v0-1" aria-label="Permalink to &quot;Namespace Resolution Protocol v0.1&quot;">​</a></h1><p><strong>neurons.me / suiGn</strong><strong>Status:</strong> Draft — Working Document <strong>License:</strong> CC0 1.0 Universal — Public Domain</p><hr><h2 id="preamble" tabindex="-1">Preamble <a class="header-anchor" href="#preamble" aria-label="Permalink to &quot;Preamble&quot;">​</a></h2><p>This document specifies the <strong>Namespace Resolution Protocol (NRP)</strong> for the <code>me://</code> URI scheme.</p><p>The NRP defines how a <code>me://</code> URI is resolved from a symbolic address into a concrete semantic value, across a distributed mesh of surfaces — without a central registry, without a central server, and without requiring persistent connectivity.</p><p>It is the protocol that closes the gap between:</p><ul><li><p><strong>Semantic resolution</strong> — already implemented in <code>this.me</code>: local, mathematical, derivation-based. The kernel resolves <code>me.get(&quot;wallet.balance&quot;)</code> entirely offline.</p></li><li><p><strong>Topological resolution</strong> — the new work: how a requesting surface finds and reaches the surface that holds the target namespace, path, and key material.</p><p>These two layers are separate concerns, implemented separately, but always composed in that order:</p></li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[surface:iphone]/wallet.balance</span></span>
<span class="line"><span>         │                    │                 │</span></span>
<span class="line"><span>    topological           topological        semantic</span></span>
<span class="line"><span>    (find the             (find which        (resolve the</span></span>
<span class="line"><span>     namespace)            surface)           path locally)</span></span></code></pre></div><p>The NRP specifies the topological layer. The semantic layer is already specified by <code>this.me</code>.</p><hr><h2 id="_1-definitions" tabindex="-1">1. Definitions <a class="header-anchor" href="#_1-definitions" aria-label="Permalink to &quot;1. Definitions&quot;">​</a></h2><p><strong>Namespace</strong> — A named semantic domain. Represented as a human-readable label (e.g., <code>jabellae.cleaker.me</code>). A namespace is owned by whoever holds its root key material. There is no central authority that grants or revokes a namespace.</p><p><strong>Surface</strong> — A physical or logical runtime context that can hold a <code>.me</code> kernel instance and participate in the mesh. Examples: an iPhone app, a MacBook daemon, a server process, a browser tab. A surface has a name within a namespace.</p><p><strong>Surface identity</strong> — The cryptographic identifier of a surface within a namespace:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>surface_id = hash(namespace_key, surface_name)</span></span></code></pre></div><p>This is never the cleartext <code>surface_name</code>. Only holders of <code>namespace_key</code> can derive or verify a <code>surface_id</code>.</p><p><strong>Namespace key</strong> — The root secret from which all key material in a namespace is derived. Equivalent to the seed in the derivation-based identity model. Never transmitted; always local.</p><p><strong>Endpoint descriptor</strong> — A transport-layer locator for a surface (IP:port, relay address, onion address, etc.). Stored encrypted inside the surface index. Not the surface&#39;s identity — just how to reach it right now.</p><p><strong>Surface index</strong> — A <code>.me</code> secret space, scoped under the namespace key, that maps surface identities to their current endpoint descriptors. It is the mesh&#39;s contact book. Every surface that holds the namespace key can read and write it.</p><p><strong>Claim token</strong> — A one-time, time-limited token that authorizes a new surface to join a namespace. Generated by an existing surface. Expires. Consumed on first use.</p><p><strong>Stealth root</strong> — A secret scope root that returns <code>undefined</code> on resolution. This is an honest absence, not an error. The NRP must preserve this distinction at the network level.</p><p><strong>Semantic island</strong> — A self-contained <code>.me</code> kernel instance holding a fragment of a namespace. Islands do not need to be constantly connected. They interact by exchanging mathematical hashes, not cleartext data.</p><hr><h2 id="_2-the-two-layer-model" tabindex="-1">2. The Two-Layer Model <a class="header-anchor" href="#_2-the-two-layer-model" aria-label="Permalink to &quot;2. The Two-Layer Model&quot;">​</a></h2><p>Resolution of a <code>me://</code> URI proceeds in two phases, always in this order.</p><h3 id="phase-1-—-topological-resolution" tabindex="-1">Phase 1 — Topological resolution <a class="header-anchor" href="#phase-1-—-topological-resolution" aria-label="Permalink to &quot;Phase 1 — Topological resolution&quot;">​</a></h3><p><strong>Input:</strong> <code>namespace</code> + <code>selector</code><strong>Output:</strong> a reachable surface endpoint</p><p>The requesting surface must find the surface that can serve the requested path. This involves:</p><ol><li><p>Deriving the target <code>surface_id</code> from the local namespace key and the selector&#39;s surface name.</p></li><li><p>Looking up the endpoint descriptor for that <code>surface_id</code> in the local surface index.</p></li><li><p>Establishing a connection to that endpoint.</p><p>If the surface index does not contain the target <code>surface_id</code>, or if the target surface is unreachable, topological resolution fails. The request does not proceed to Phase 2.</p></li></ol><h3 id="phase-2-—-semantic-resolution" tabindex="-1">Phase 2 — Semantic resolution <a class="header-anchor" href="#phase-2-—-semantic-resolution" aria-label="Permalink to &quot;Phase 2 — Semantic resolution&quot;">​</a></h3><p><strong>Input:</strong> <code>path</code> + key material (if the path is within a secret scope) <strong>Output:</strong> the resolved value, or <code>undefined</code>, or a closed failure</p><p>Once the requesting surface has a connection to the target surface, it sends a read request for the path. The target surface resolves the path locally using its <code>.me</code> kernel, and returns a <strong>disclosure envelope</strong> (see Section 6).</p><p>Semantic resolution is entirely local to the target surface. The requesting surface never sees the target&#39;s kernel state directly — only the disclosure envelope for the specific path requested.</p><hr><h2 id="_3-surface-identity-and-the-surface-index" tabindex="-1">3. Surface Identity and the Surface Index <a class="header-anchor" href="#_3-surface-identity-and-the-surface-index" aria-label="Permalink to &quot;3. Surface Identity and the Surface Index&quot;">​</a></h2><h3 id="_3-1-surface-identity-derivation" tabindex="-1">3.1 Surface identity derivation <a class="header-anchor" href="#_3-1-surface-identity-derivation" aria-label="Permalink to &quot;3.1 Surface identity derivation&quot;">​</a></h3><p>A surface&#39;s identity within a namespace is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>surface_id = HMAC-SHA256(namespace_key, &quot;surface:&quot; + surface_name)</span></span></code></pre></div><p>This produces a 32-byte opaque identifier. It is the surface&#39;s public identity on the mesh. Parties without the <code>namespace_key</code> cannot derive it, verify it, or enumerate surfaces in the namespace.</p><p>The <code>surface_name</code> is a human-readable label chosen by the surface operator (e.g., <code>&quot;iphone&quot;</code>, <code>&quot;macbook&quot;</code>, <code>&quot;daemon-home&quot;</code>). It is meaningful only to namespace holders.</p><h3 id="_3-2-surface-index-structure" tabindex="-1">3.2 Surface index structure <a class="header-anchor" href="#_3-2-surface-index-structure" aria-label="Permalink to &quot;3.2 Surface index structure&quot;">​</a></h3><p>The surface index is a <code>.me</code> secret space:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;_&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">](</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;namespace_key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// For each known surface:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[surface_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;iphone&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[surface_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">endpoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;encrypted_endpoint_descriptor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[surface_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">last_seen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(timestamp);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.surfaces[surface_id].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">public_key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;surface_ephemeral_pubkey&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>The index is stored locally on every surface that holds the namespace key. There is no canonical remote copy. Surfaces sync the index through the mesh on reconnection (see Section 7).</p><p>The <code>endpoint_descriptor</code> field is doubly encrypted: first under the namespace key (so only namespace members can read it), and second it may use an ephemeral surface key for transport security. The endpoint is volatile and may change. The <code>surface_id</code> is stable.</p><h3 id="_3-3-reading-from-the-surface-index" tabindex="-1">3.3 Reading from the surface index <a class="header-anchor" href="#_3-3-reading-from-the-surface-index" aria-label="Permalink to &quot;3.3 Reading from the surface index&quot;">​</a></h3><p>A surface resolves <code>me://[surface:iphone]/some.path</code> as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Compute: target_id = HMAC-SHA256(namespace_key, &quot;surface:iphone&quot;)</span></span>
<span class="line"><span>2. Read: endpoint = me(&quot;surfaces.&quot; + target_id + &quot;.endpoint&quot;)</span></span>
<span class="line"><span>3. If endpoint is undefined → surface not found → fail with NRP_ERROR_SURFACE_NOT_FOUND</span></span>
<span class="line"><span>4. Decrypt endpoint descriptor → get transport address</span></span>
<span class="line"><span>5. Proceed to Phase 2 (semantic resolution)</span></span></code></pre></div><hr><h2 id="_4-the-claim-ceremony" tabindex="-1">4. The Claim Ceremony <a class="header-anchor" href="#_4-the-claim-ceremony" aria-label="Permalink to &quot;4. The Claim Ceremony&quot;">​</a></h2><p>The claim ceremony is how a new surface joins a namespace. It replaces the need for a central authority to &quot;add&quot; a surface.</p><p>The current implementation has a claim token as a temporary in-memory value. This section formalizes and hardens it.</p><h3 id="_4-1-token-generation" tabindex="-1">4.1 Token generation <a class="header-anchor" href="#_4-1-token-generation" aria-label="Permalink to &quot;4.1 Token generation&quot;">​</a></h3><p>An existing surface in the namespace (the <strong>inviting surface</strong>) generates a claim token:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>nonce         = random_bytes(16)</span></span>
<span class="line"><span>expiry        = now() + TTL            // recommended TTL: 300 seconds</span></span>
<span class="line"><span>claim_token   = HMAC-SHA256(namespace_key, nonce + expiry)</span></span>
<span class="line"><span>claim_payload = base64url(nonce + expiry + claim_token)</span></span></code></pre></div><p>The claim payload is encoded into the URI:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[claim:CLAIM_PAYLOAD]/new-surface</span></span></code></pre></div><p>And optionally rendered as a QR code for physical proximity pairing (already implemented in Cleaker).</p><h3 id="_4-2-token-verification" tabindex="-1">4.2 Token verification <a class="header-anchor" href="#_4-2-token-verification" aria-label="Permalink to &quot;4.2 Token verification&quot;">​</a></h3><p>The <strong>new surface</strong> presents the claim payload. The inviting surface (or any namespace surface that receives the pairing request) verifies:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Decode claim_payload → extract nonce, expiry, presented_token</span></span>
<span class="line"><span>2. Check: now() &lt; expiry → if expired, reject with NRP_ERROR_CLAIM_EXPIRED</span></span>
<span class="line"><span>3. Recompute: expected_token = HMAC-SHA256(namespace_key, nonce + expiry)</span></span>
<span class="line"><span>4. Check: presented_token == expected_token → if not, reject with NRP_ERROR_CLAIM_INVALID</span></span>
<span class="line"><span>5. Check: token has not been used before (nonce registry, local) → if used, reject with NRP_ERROR_CLAIM_CONSUMED</span></span></code></pre></div><h3 id="_4-3-key-handshake" tabindex="-1">4.3 Key handshake <a class="header-anchor" href="#_4-3-key-handshake" aria-label="Permalink to &quot;4.3 Key handshake&quot;">​</a></h3><p>After verification, the inviting surface and new surface perform a key handshake:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. New surface generates an ephemeral keypair: (eph_pub, eph_priv)</span></span>
<span class="line"><span>2. New surface sends eph_pub to inviting surface</span></span>
<span class="line"><span>3. Inviting surface encrypts namespace_key with eph_pub:</span></span>
<span class="line"><span>     encrypted_namespace_key = ECIES(eph_pub, namespace_key)</span></span>
<span class="line"><span>4. Inviting surface sends encrypted_namespace_key + its own surface_id + current surface index snapshot</span></span>
<span class="line"><span>5. New surface decrypts with eph_priv → receives namespace_key and surface index</span></span>
<span class="line"><span>6. New surface registers itself in the surface index:</span></span>
<span class="line"><span>     surface_id_new = HMAC-SHA256(namespace_key, &quot;surface:&quot; + new_surface_name)</span></span>
<span class="line"><span>     me.surfaces[surface_id_new].endpoint(...)</span></span>
<span class="line"><span>     me.surfaces[surface_id_new].public_key(eph_pub)</span></span>
<span class="line"><span>7. New surface broadcasts its registration to all online surfaces (see Section 5.3)</span></span></code></pre></div><h3 id="_4-4-token-consumption" tabindex="-1">4.4 Token consumption <a class="header-anchor" href="#_4-4-token-consumption" aria-label="Permalink to &quot;4.4 Token consumption&quot;">​</a></h3><p>The nonce is added to a local consumed-nonce registry after a successful claim. The registry is pruned of entries older than the maximum TTL. This prevents replay attacks.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">me.system.consumed_nonces[nonce].</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">at</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(timestamp);</span></span></code></pre></div><hr><h2 id="_5-selector-resolution-rules" tabindex="-1">5. Selector Resolution Rules <a class="header-anchor" href="#_5-selector-resolution-rules" aria-label="Permalink to &quot;5. Selector Resolution Rules&quot;">​</a></h2><p>The <code>me://</code> URI selector determines how topological resolution proceeds.</p><h3 id="_5-1-current-—-current-surface" tabindex="-1">5.1 <code>[current]</code> — current surface <a class="header-anchor" href="#_5-1-current-—-current-surface" aria-label="Permalink to &quot;5.1 \`[current]\` — current surface&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[current]/profile.name</span></span>
<span class="line"><span>// or equivalently:</span></span>
<span class="line"><span>me://profile.name</span></span></code></pre></div><p>Resolves entirely locally on the surface receiving the request. No topological resolution needed. Phase 1 is a no-op.</p><h3 id="_5-2-surface-name-—-specific-surface" tabindex="-1">5.2 <code>[surface:name]</code> — specific surface <a class="header-anchor" href="#_5-2-surface-name-—-specific-surface" aria-label="Permalink to &quot;5.2 \`[surface:name]\` — specific surface&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[surface:iphone]/runtime.battery</span></span></code></pre></div><ol><li>Derive <code>target_id = HMAC-SHA256(namespace_key, &quot;surface:iphone&quot;)</code></li><li>Look up endpoint in surface index</li><li>If not found: <code>NRP_ERROR_SURFACE_NOT_FOUND</code></li><li>If found but unreachable: <code>NRP_ERROR_SURFACE_UNREACHABLE</code></li><li>If found and reachable: proceed to semantic resolution</li></ol><h3 id="_5-3-—-broadcast-to-all-surfaces" tabindex="-1">5.3 <code>[]</code> — broadcast to all surfaces <a class="header-anchor" href="#_5-3-—-broadcast-to-all-surfaces" aria-label="Permalink to &quot;5.3 \`[]\` — broadcast to all surfaces&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[]/chat</span></span></code></pre></div><ol><li><p>Read the full surface index</p></li><li><p>Iterate all known <code>surface_id</code> entries</p></li><li><p>For each entry, attempt to reach the endpoint</p></li><li><p>Send the request to all reachable surfaces</p></li><li><p>Collect responses (or timeout)</p></li><li><p>Offline surfaces are skipped — broadcast is best-effort</p></li><li><p>The requesting surface may optionally queue undelivered messages for retry (implementation-defined)</p><p>Broadcast does <strong>not</strong> wait for all surfaces to respond. It fires and collects what it can within a configurable timeout window.</p></li></ol><h3 id="_5-4-claim-token-—-pairing-handshake" tabindex="-1">5.4 <code>[claim:token]</code> — pairing handshake <a class="header-anchor" href="#_5-4-claim-token-—-pairing-handshake" aria-label="Permalink to &quot;5.4 \`[claim:token]\` — pairing handshake&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me://jabellae.cleaker.me[claim:CLAIM_PAYLOAD]/new-surface</span></span></code></pre></div><p>Triggers the claim ceremony (Section 4). Does not proceed to semantic resolution — the claim selector is a control-plane operation, not a data-plane operation.</p><hr><h2 id="_6-the-disclosure-model" tabindex="-1">6. The Disclosure Model <a class="header-anchor" href="#_6-the-disclosure-model" aria-label="Permalink to &quot;6. The Disclosure Model&quot;">​</a></h2><p>This is what a target surface returns for each category of path request. This model must be implemented consistently across all surfaces. A surface that leaks structural information in error responses breaks the security model.</p><h3 id="_6-1-public-path" tabindex="-1">6.1 Public path <a class="header-anchor" href="#_6-1-public-path" aria-label="Permalink to &quot;6.1 Public path&quot;">​</a></h3><p>The path exists and is not within any secret scope.</p><p><strong>Returns:</strong> the resolved value.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;profile.name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;José Abella&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;origin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;public&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_6-2-stealth-root" tabindex="-1">6.2 Stealth root <a class="header-anchor" href="#_6-2-stealth-root" aria-label="Permalink to &quot;6.2 Stealth root&quot;">​</a></h3><p>The path points to the root of a secret scope (e.g., <code>wallet</code> when <code>wallet</code> is declared with <code>[&quot;_&quot;]</code>).</p><p><strong>Returns:</strong> <code>undefined</code>. Explicitly. Not a 404, not an error, not a &quot;path not found.&quot; The absence is honest and intentional.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wallet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;origin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stealth&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The distinction between <code>null</code> (stealth root) and <code>NRP_ERROR_PATH_NOT_FOUND</code> (path does not exist at all) is architectural. Callers must not be able to distinguish &quot;this path is secret&quot; from &quot;this path does not exist&quot; — both return a form of <code>undefined</code>. The implementation choice of <code>null</code> vs. omitting the field is left to the transport binding, but the semantics must be indistinguishable to an observer without the secret key.</p><h3 id="_6-3-secret-leaf-—-correct-key" tabindex="-1">6.3 Secret leaf — correct key <a class="header-anchor" href="#_6-3-secret-leaf-—-correct-key" aria-label="Permalink to &quot;6.3 Secret leaf — correct key&quot;">​</a></h3><p>The path is within a secret scope, and the requesting surface presents the correct key (via the <code>secret:key@</code> prefix in the URI, or through a pre-established shared secret).</p><p><strong>Returns:</strong> the resolved value.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wallet.balance&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12480</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;origin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stealth&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_6-4-secret-leaf-—-wrong-or-absent-key" tabindex="-1">6.4 Secret leaf — wrong or absent key <a class="header-anchor" href="#_6-4-secret-leaf-—-wrong-or-absent-key" aria-label="Permalink to &quot;6.4 Secret leaf — wrong or absent key&quot;">​</a></h3><p>The path is within a secret scope, and the requesting surface does not present the correct key, or presents no key.</p><p><strong>Returns:</strong> same envelope as stealth root. The response must be indistinguishable from case 6.2. A wrong key must not produce a different error than no key.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wallet.balance&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;origin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stealth&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This is the &quot;fail closed, leak nothing&quot; invariant. It directly mirrors the <code>[&quot;_&quot;]</code> axiom in the <code>.me</code> kernel (A0 + A2).</p><h3 id="_6-5-path-does-not-exist" tabindex="-1">6.5 Path does not exist <a class="header-anchor" href="#_6-5-path-does-not-exist" aria-label="Permalink to &quot;6.5 Path does not exist&quot;">​</a></h3><p>The path is not declared in the kernel and is not within any secret scope.</p><p><strong>Returns:</strong></p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;not_found&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;does.not.exist&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This is the only case where a <code>not_found</code> status is returned. It must only be returned for genuinely absent paths that are not near any secret scope. If there is any ambiguity about whether a path might be secret, return the stealth envelope (6.2) instead.</p><hr><h2 id="_7-surface-index-synchronization" tabindex="-1">7. Surface Index Synchronization <a class="header-anchor" href="#_7-surface-index-synchronization" aria-label="Permalink to &quot;7. Surface Index Synchronization&quot;">​</a></h2><p>The surface index is local on each surface. When surfaces reconnect after being offline, they may have divergent views of the index. The protocol uses a simple Last-Write-Wins (LWW) strategy, consistent with the <code>.me</code> kernel&#39;s A9 axiom (deterministic conflict resolution).</p><h3 id="_7-1-sync-on-reconnect" tabindex="-1">7.1 Sync on reconnect <a class="header-anchor" href="#_7-1-sync-on-reconnect" aria-label="Permalink to &quot;7.1 Sync on reconnect&quot;">​</a></h3><p>When surface A reconnects to surface B:</p><ol><li>A sends its surface index version vector (a map of <code>surface_id → last_seen_timestamp</code>)</li><li>B compares against its own version vector</li><li>Each sends the other the entries that are newer on its side</li><li>Both apply the updates using LWW on <code>last_seen</code> timestamp</li></ol><h3 id="_7-2-surface-expiry" tabindex="-1">7.2 Surface expiry <a class="header-anchor" href="#_7-2-surface-expiry" aria-label="Permalink to &quot;7.2 Surface expiry&quot;">​</a></h3><p>A surface entry that has not updated its <code>last_seen</code> timestamp within a configurable window (e.g., 30 days) may be marked stale. Stale surfaces are kept in the index but deprioritized in routing. They are not deleted automatically — deletion requires an explicit <code>[&quot;-&quot;]</code> operation by a namespace holder.</p><h3 id="_7-3-endpoint-volatility" tabindex="-1">7.3 Endpoint volatility <a class="header-anchor" href="#_7-3-endpoint-volatility" aria-label="Permalink to &quot;7.3 Endpoint volatility&quot;">​</a></h3><p>Endpoint descriptors are volatile. A surface&#39;s IP address or relay address may change frequently. Each surface is responsible for updating its own endpoint descriptor in the index whenever its transport address changes, and broadcasting the update to all online surfaces.</p><hr><h2 id="_8-error-reference" tabindex="-1">8. Error Reference <a class="header-anchor" href="#_8-error-reference" aria-label="Permalink to &quot;8. Error Reference&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>NRP_ERROR_SURFACE_NOT_FOUND</code></td><td><code>surface_id</code> not in local index</td></tr><tr><td><code>NRP_ERROR_SURFACE_UNREACHABLE</code></td><td>surface_id found but endpoint not reachable</td></tr><tr><td><code>NRP_ERROR_CLAIM_EXPIRED</code></td><td>claim token TTL exceeded</td></tr><tr><td><code>NRP_ERROR_CLAIM_INVALID</code></td><td>HMAC verification failed</td></tr><tr><td><code>NRP_ERROR_CLAIM_CONSUMED</code></td><td>nonce already used</td></tr><tr><td><code>NRP_ERROR_NAMESPACE_UNKNOWN</code></td><td>no namespace key available for this namespace</td></tr><tr><td><code>NRP_ERROR_PATH_NOT_FOUND</code></td><td>path does not exist and is not near any secret scope</td></tr><tr><td><code>NRP_ERROR_TRANSPORT</code></td><td>network-level failure (connection refused, timeout, etc.)</td></tr></tbody></table><hr><h2 id="_9-what-is-not-specified-here-v0-1-scope" tabindex="-1">9. What Is Not Specified Here (v0.1 scope) <a class="header-anchor" href="#_9-what-is-not-specified-here-v0-1-scope" aria-label="Permalink to &quot;9. What Is Not Specified Here (v0.1 scope)&quot;">​</a></h2><p>The following are intentionally deferred to later versions:</p><p><strong>Transport binding</strong> — This document specifies the protocol semantics, not the wire format. A v0.2 transport binding document will specify the exact message encoding (JSON-over-WebSocket, protobuf, etc.) and the TLS/noise handshake for the surface-to-surface connection.</p><p><strong>Relay infrastructure</strong> — When two surfaces cannot reach each other directly (NAT, firewall, different networks), a relay is needed. The relay protocol is not specified here. The relay must not be able to read message content — it is a blind forwarder. Relay discovery and selection are deferred.</p><p><strong>Namespace key distribution across surfaces</strong> — This document assumes the namespace key is already present on the requesting surface. The initial key bootstrap (how the first surface gets the namespace key from the derivation seed) is specified by the <code>.me</code> kernel, not the NRP. Subsequent key sharing is handled by the claim ceremony (Section 4).</p><p><strong>Multi-namespace resolution</strong> — A surface may hold keys for multiple namespaces. How a surface selects the correct namespace key when a URI omits the namespace qualifier is implementation-defined in v0.1.</p><p><strong>Revocation</strong> — How to revoke a surface from a namespace (e.g., a lost device) without a central authority is an open problem. A candidate mechanism is a signed revocation entry in the surface index, broadcast to all surfaces. Deferred to v0.2.</p><hr><h2 id="_10-design-principles" tabindex="-1">10. Design Principles <a class="header-anchor" href="#_10-design-principles" aria-label="Permalink to &quot;10. Design Principles&quot;">​</a></h2><p>These are the invariants that any implementation must preserve. They are not implementation details — they are the protocol&#39;s soul.</p><p><strong>1. No central registry.</strong> The surface index is local to each namespace holder. Discovery requires holding the namespace key. There is nothing to hack that reveals a complete list of surfaces or identities.</p><p><strong>2. Identity is computation, not storage.</strong> A surface&#39;s identity is derived from a key and a name. It is not assigned by an authority and not stored in a global directory.</p><p><strong>3. Honest absence.</strong> A stealth root returns <code>undefined</code>, not an error. The protocol does not reveal the existence of secret scopes to parties without the key. Absence and secrecy are indistinguishable from the outside.</p><p><strong>4. Fail closed.</strong> Wrong keys produce the same response as absent keys. The protocol leaks no information about why a resolution returned <code>undefined</code>.</p><p><strong>5. Minimum surface area.</strong> Surfaces exchange only what is necessary. A broadcast sends a path read request — not the full kernel state, not the surface index, not any metadata beyond what the request requires.</p><p><strong>6. Mathematical fragmentation.</strong> No single surface holds a complete picture of any namespace. The identity is distributed across surfaces by design. Stealing one surface does not compromise the namespace.</p><p><strong>7. Offline-capable.</strong> Local resolution (<code>[current]</code> or no selector) works with no network access. The protocol degrades gracefully: offline surfaces are skipped in broadcast, missing surfaces return <code>NRP_ERROR_SURFACE_UNREACHABLE</code>. The local kernel is never blocked by network state.</p><hr><h2 id="appendix-a-—-formal-grammar-abnf-from-uri-scheme-v1" tabindex="-1">Appendix A — Formal Grammar (ABNF, from URI scheme v1) <a class="header-anchor" href="#appendix-a-—-formal-grammar-abnf-from-uri-scheme-v1" aria-label="Permalink to &quot;Appendix A — Formal Grammar (ABNF, from URI scheme v1)&quot;">​</a></h2><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me-uri       = &quot;me://&quot; [ namespace ] [ selector ] [ &quot;/&quot; path ]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>namespace    = 1*( ALPHA / DIGIT / &quot;.&quot; / &quot;_&quot; / &quot;-&quot; )</span></span>
<span class="line"><span>selector     = &quot;[&quot; ( &quot;current&quot; / &quot;&quot; / &quot;surface:&quot; surface-name / &quot;claim:&quot; token ) &quot;]&quot;</span></span>
<span class="line"><span>surface-name = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span>
<span class="line"><span>token        = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span>
<span class="line"><span>path         = *( VCHAR / &quot;/&quot; )</span></span></code></pre></div><p>The <code>secret:key@namespace</code> prefix for authenticated access is parsed as:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>me-uri-auth  = &quot;me://&quot; &quot;secret:&quot; secret-key &quot;@&quot; namespace [ selector ] [ &quot;/&quot; path ]</span></span>
<span class="line"><span>secret-key   = 1*( ALPHA / DIGIT / &quot;-&quot; / &quot;_&quot; )</span></span></code></pre></div><hr><h2 id="appendix-b-—-open-questions-for-v0-2" tabindex="-1">Appendix B — Open Questions for v0.2 <a class="header-anchor" href="#appendix-b-—-open-questions-for-v0-2" aria-label="Permalink to &quot;Appendix B — Open Questions for v0.2&quot;">​</a></h2><p>These are the questions this document intentionally leaves open, in priority order:</p><ol><li><p><strong>Relay protocol</strong> — How do two surfaces on different networks find a common relay, and how does the relay stay blind?</p></li><li><p><strong>Surface revocation</strong> — How does a namespace holder revoke a lost or compromised surface without a central authority?</p></li><li><p><strong>Namespace discovery</strong> — Can a surface discover other namespaces it does not already hold keys for? (Current answer: no. Is this the right answer forever?)</p></li><li><p><strong>Index consistency under partition</strong> — If the mesh is partitioned for a long time and surfaces update their own entries, what is the merge strategy beyond LWW?</p></li><li><p><strong>Derivation-based namespace bootstrap</strong> — Should the namespace key be derivable from a BIP-39 mnemonic or similar standard seed phrase, so it can be reconstructed without any network access?</p></li><li><p><strong>Cross-namespace pointers</strong> — The <code>[&quot;-&gt;&quot;]</code> operator in <code>.me</code> can create semantic links between namespaces. How does the NRP resolve a path that crosses namespace boundaries?</p></li></ol><hr><p><strong>∴ Witness our seal</strong><strong>suiGn / neurons.me</strong><strong>v0.1 — Draft</strong></p>`,151)])])}const k=a(i,[["render",o]]);export{u as __pageData,k as default};
